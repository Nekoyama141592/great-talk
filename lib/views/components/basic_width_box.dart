import 'package:flutter/material.dart';

class BasicWidthBox extends StatelessWidget {
  const BasicWidthBox({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 20.0,
    );
  }
}
