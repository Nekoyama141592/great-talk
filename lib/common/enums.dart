enum InterlocutorType { userContent, originalContent }

enum MessageType { text }

enum PrefsKey {
  // personIdで各々のChat履歴
  chatCount,
  initialPeople,
  isAgreedToTerms,
  isV3initialized,
  lastChatDate,
}

enum TokenType { following }
